# fpcssim Frontend (JavaScript/HTML)
If you are running the Live Server extension (included in the VS Code dev container), open `index.html` and run the `Open with Live Server` command from the right-click context menu.

The frontend runs on port 5500: http://localhost:5500.
